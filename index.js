import { createPaymentsForAllResidents } from "./model/payment.model.js";

// const res = await createPaymentsForAllResidents(600, 20, );

const dueDate = new Date("2026-05-07");

// const res = await createPaymentsForAllResidents(600, 20, dueDate, "MAY");

console.log(res);
